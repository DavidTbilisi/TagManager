{
  "mcpServers": {
    "filetagger": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "filetagger.mcp_stdio"],
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
}
